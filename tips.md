```
browser-sync start --server --directory --files "**/*

= (bidirectional binding)
@ attribute binding
< one way binding
& reference binding


(condition) ? a : b
if condition true use one value otherwise b
```
